import React from 'react';
import {createStackNavigator} from '@react-navigation/stack';
import {
  FlatList,
  View,
  Text,
  StyleSheet,
  StatusBar,
  SafeAreaView,
  Image,
  Pressable,
  TouchableOpacity,
} from 'react-native';

const Stack = createStackNavigator();

export default function ProductLikeScreen() {
  return <Text>ProductLikeScreen</Text>;
}
